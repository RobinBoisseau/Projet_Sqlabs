import { ParticipationEntite } from './participation-entite';

describe('ParticipationEntite', () => {
  it('should create an instance', () => {
    expect(new ParticipationEntite()).toBeTruthy();
  });
});
